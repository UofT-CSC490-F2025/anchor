Starter assets and default logos are moved to `legacy-starter` to keep the main `images` folder clean.

If you want to restore them, move the files from `assets/legacy-starter` back into `assets/images`.
